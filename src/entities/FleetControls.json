{
  "name": "FleetControls",
  "type": "object",
  "properties": {
    "fleet_id": {
      "type": "string",
      "description": "Reference to Fleet entity"
    },
    "max_preauth_amount": {
      "type": "number",
      "description": "Maximum dollar amount that can be pre-authorized without approval",
      "default": 500
    },
    "require_approval_above": {
      "type": "number",
      "description": "Dollar amount above which approval is required",
      "default": 1000
    },
    "part_pricing": {
      "type": "array",
      "description": "Custom pricing and variance settings for specific parts",
      "items": {
        "type": "object",
        "properties": {
          "part_name": {
            "type": "string"
          },
          "category": {
            "type": "string"
          },
          "expected_price": {
            "type": "number"
          },
          "variance_percentage": {
            "type": "number",
            "description": "Allowed variance percentage (e.g., 10 means 10% higher is still authorized)"
          }
        }
      },
      "default": []
    },
    "auto_approve_within_variance": {
      "type": "boolean",
      "description": "Automatically approve services if price is within variance",
      "default": true
    }
  },
  "required": [
    "fleet_id"
  ]
}