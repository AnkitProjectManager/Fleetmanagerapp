{
  "name": "Administrator",
  "type": "object",
  "properties": {
    "fleet_id": {
      "type": "string",
      "description": "Reference to Fleet entity"
    },
    "user_email": {
      "type": "string",
      "format": "email",
      "description": "Email of the user who is an administrator"
    },
    "user_name": {
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "is_primary": {
      "type": "boolean",
      "default": false,
      "description": "Whether this is the primary fleet manager"
    }
  },
  "required": [
    "fleet_id",
    "user_email",
    "user_name"
  ]
}